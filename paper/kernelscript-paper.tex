\documentclass[sigplan,10pt,nonacm]{acmart}

\usepackage{amsmath}
\usepackage{array}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{float}
\usepackage{listings}
\usepackage{needspace}
\usepackage{placeins}
\usepackage{xcolor}

\renewcommand\footnotetextcopyrightpermission[1]{}
\settopmatter{printfolios=true}
\setlength{\emergencystretch}{1.5em}
\setlength{\textfloatsep}{8pt plus 2pt minus 2pt}
\setlength{\floatsep}{6pt plus 2pt minus 2pt}
\setlength{\intextsep}{8pt plus 2pt minus 2pt}

\setlist{nosep,leftmargin=*}
\lstset{
  basicstyle=\ttfamily\footnotesize,
  columns=fullflexible,
  breaklines=true,
  frame=single,
  xleftmargin=0.2em,
  xrightmargin=0.2em
}

\input{../results/paper_numbers}

\newcommand{\tool}{BPFScript}
\newcommand{\code}[1]{\texttt{\detokenize{#1}}}

\title{\tool: Compiler-Checked Cross-Boundary Typed DSL for eBPF Applications}
% Replace TBD with the actual submission-system paper ID before submitting.
\author{Paper ID: 29}
\renewcommand{\shortauthors}{Paper ID: TBD}

\begin{abstract}
eBPF has become the standard mechanism for extending Linux with custom packet
processing, tracing, and security policies, but its programming model is
fragmented. A single application spans kernel code, a userspace loader, and
shared maps, yet the relationships among them (types, lifecycle ordering,
shared state) are checked only at load time or not at all, leading to late,
hard-to-diagnose failures. We observe that these cross-boundary relationships
are duplicated concepts that a type system can unify. We present \tool{}, a 
DSL that types maps, program handles, and
execution domains in one source, then lowers to standard C through the
clang/bpftool/libbpf toolchain. The compiler detects mismatches, such as wrong
context types or misordered lifecycle calls, before code generation. Generated
projects load, attach, and pass traffic on a real kernel, showing that eBPF's
cross-boundary structure can be typed while preserving the existing toolchain.
\end{abstract}

\ccsdesc[500]{Software and its engineering~Domain specific languages}
\ccsdesc[300]{Software and its engineering~Static analysis}
\ccsdesc[300]{Computer systems organization~Reliability}
\keywords{eBPF, domain-specific language, static typing, systems}

\begin{document}
\maketitle
\pagestyle{plain}

\section{Introduction}

% P1: Context/Importance
eBPF lets developers run application-specific logic inside the Linux kernel
under a verifier that proves memory safety and bounded
execution~\cite{linux-ebpf}. It now underpins packet processing in major cloud
providers, powers observability platforms, enforces security policies, and
enables scheduler experimentation within a large and growing ecosystem.

% P2: Problem
Yet the programming model is fragmented. A single eBPF application spans kernel
code constrained by the verifier, a userspace loader that opens objects and
manages lifecycle, shared maps and ring buffers, and (for newer features)
generated skeleton headers that must match the running kernel. The relationships
among these pieces, such as a map's key and value types or the order of load,
attach, and detach, are checked only at load time or not at all. Errors surface
late as cryptic verifier rejections or silent mismatches.

% P3: Gap
Existing tools reduce boilerplate yet leave cross-boundary relationships
implicit. The standard eBPF development workflow requires hand-written kernel C,
userspace C, and a Makefile~\cite{libbpf}. Aya compiles both sides from Rust
and shares typed maps~\cite{aya}; cilium/ebpf does similarly in
Go~\cite{cilium-ebpf}. In both, lifecycle ordering remains
a runtime property. bpftrace targets tracing with higher-level
abstractions~\cite{bpftrace}, not general eBPF applications. No existing tool
types the full cross-boundary structure at compile time.

% P4: Insight
The key observation is that these relationships are single concepts duplicated
across components. A map declaration is at once a kernel data structure, a
userspace lookup target, and a type contract. A program handle is at once a
function name, a BPF section, and a file descriptor. In C/libbpf, naming
conventions connect those copies, so mismatches surface late. In one
typed source, the compiler can see and check them directly.

% P5: Solution
\tool{} realizes this idea as a compiled, multi-domain DSL. A developer writes
eBPF entry points, userspace control flow, maps, and ring buffers in one source
file. The compiler types maps, program handles, and execution domains, then
lowers to eBPF C, userspace C, and a Makefile. The libbpf and bpftool
path produces the final binaries, so the existing toolchain is preserved.

% P6: Contributions
This paper contributes:
\begin{itemize}
\item A \emph{typed model of cross-boundary eBPF structure}: execution domains,
  typed maps, and program-lifecycle handles, each with a typing rule and
  matching compiler diagnostic (Section~\ref{sec:model}).
\item A compiler that realizes this model while preserving the stock kernel
  toolchain, so compatibility failures remain visible (Section~\ref{sec:impl}).
\item An evaluation showing that each invariant triggers a diagnostic, that one source generates
  buildable libbpf projects, and that generated objects load, attach, and run
  on a real kernel (Section~\ref{sec:eval}).
\end{itemize}

\section{Background and Motivation}
\label{sec:bg}

eBPF allows user-supplied bytecode to run inside the Linux kernel after a
verifier proves memory safety and bounded execution~\cite{linux-ebpf,ebpfsurvey}.
The mechanism now underpins a wide range of applications: tracing and
profiling~\cite{bpftrace,bcc}, high-performance networking~\cite{xdp}, security
policy enforcement~\cite{lsm-bpf}, CPU scheduling~\cite{ghost}, page-cache
customization~\cite{cacheext}, GPU resource control~\cite{gpuext}, and
userspace extension runtimes~\cite{bpftime}. Every eBPF application has a
kernel program and a userspace loader that communicate through maps or ring
buffers. The standard eBPF development workflow requires developers to maintain
separate kernel C, userspace C, and a Makefile~\cite{libbpf}. Aya unifies both
sides in Rust~\cite{aya}, and Rex provides safe Rust kernel
extensions~\cite{rex}, but these tools still leave cross-boundary relationships
implicit.

The kernel/userspace split introduces three kinds of coupling that current
tools do not fully address. \emph{Lifecycle coupling} requires operations to
occur in a valid order (load before attach, detach before close), but C/libbpf
checks this only at runtime. \emph{Representation coupling} requires a map's
key and value types to agree across kernel code, generated headers, and
userspace lookups, and a mismatch is silent until something fails.
\emph{Compatibility coupling} arises because generated bindings depend on the
exact kernel and libbpf versions. The verifier detects many errors, though
only at load time. A DSL can move lifecycle and representation checks to
compile time.

\section{A Typed Model of Cross-Boundary Structure}
\label{sec:model}

We describe the type system informally because a full semantics or soundness
proof is beyond scope. Every rule corresponds to a diagnostic exercised by
the rejection suite of Section~\ref{sec:eval}.

A \tool{} program is a flat sequence of declarations. Listing~\ref{lst:min}
shows the minimal shape of an application, and the rest of this section makes
its three relationships precise.

\begin{lstlisting}[language=C,caption={A unified-source eBPF application.},label={lst:min}]
include "xdp.kh"

@xdp fn pass_all(ctx: *xdp_md) -> xdp_action {
  return XDP_PASS
}

fn main() -> i32 {
  var prog = load(pass_all)
  attach(prog, "lo", 0)
  detach(prog)
  return 0
}
\end{lstlisting}

To address representation coupling, each function carries an attribute that
assigns it to an execution domain
$d \in \{\textit{user}, \textit{ebpf}, \textit{helper}, \textit{kfunc}\}$.
The attributes \code{@xdp}, \code{@tc}, \code{@probe}, \code{@tracepoint}, and
\code{@perf_event} mark eBPF entry points, while \code{@helper} marks
kernel-shared eBPF code and \code{@kfunc} marks kernel-module code. An
unattributed \code{fn} is userspace. The checker tags each function with its
domain and rejects illegal crossings, such as a userspace function calling an
\code{@helper}. Each eBPF attribute also fixes a context and return type: for
example, \code{@xdp} requires \code{ctx:*xdp_md} returning \code{xdp_action},
and \code{@tc} requires \code{*__sk_buff} returning \code{i32}. The compiler
rejects any signature that disagrees with its attribute before code generation,
so the same surface syntax remains domain-aware: userspace can allocate and
print freely, while eBPF code must obey stack and helper restrictions.
struct\_ops callbacks fit the same scheme, with context and return types
determined by the registered slot.

To address representation coupling for shared state, a map is declared as a
typed global (\code{var m : hash<K,V>(N)}) rather than a C section plus
separate userspace lookup code. The compiler checks element types at every
access site on both sides of the kernel boundary: an index \code{m[k]} requires
\code{k:K} and yields \code{V}, in eBPF and userspace alike. A wrong key type,
wrong value type, or access to an undeclared map is a compile-time error. One
declaration thus replaces kernel definitions, generated fields, and userspace
lookups that otherwise must agree by hand.

To address lifecycle coupling, the type system distinguishes a
\code{FunctionRef} (a reference to an \code{@}-attributed function) from a
\code{ProgramHandle} (a loaded program). The lifecycle builtins are typed so
that only \code{load} produces a \code{ProgramHandle}, and the formation rule
for \code{FunctionRef} connects lifecycle to domains: only an eBPF entry-point
function inhabits \code{FunctionRef}, so an \code{@helper}, \code{@kfunc}, or
userspace function cannot be passed to \code{load}.
\begin{gather*}
\frac{f \text{ has an eBPF entry attribute}}
     {\Gamma \vdash f : \texttt{FunctionRef}}
\\[3pt]
\frac{\Gamma \vdash f : \texttt{FunctionRef}}
     {\Gamma \vdash \texttt{load}(f) : \texttt{ProgramHandle}}
\\[3pt]
\frac{\Gamma \vdash h : \texttt{ProgramHandle}}
     {\Gamma \vdash \texttt{detach}(h) : \texttt{unit}}
\\[3pt]
\frac{\Gamma \vdash h : \texttt{ProgramHandle}}
     {\Gamma \vdash \texttt{attach}(h, t, \mathit{fl}) : \texttt{u32}}
\end{gather*}
Because \code{attach} and \code{detach} require a \code{ProgramHandle} and only
\code{load} produces one, ``attach before load'' is a \emph{type} error, not a
runtime failure. Passing a bare \code{FunctionRef}, an integer, or a string to
a lifecycle builtin does not type-check. The typing rules enforce a
producer/consumer handle discipline: because only \code{load} produces a
\code{ProgramHandle}, the system enforces the single ordering constraint
$\textit{load} \prec \{\textit{attach},\,\textit{detach}\}$ rather than the
complete load-to-attach-to-detach protocol. It guarantees that lifecycle
builtins receive values of the right kind, but not full path-sensitive
properties such as use-after-detach, double-attach, or leaked handles.

The benefit is earlier, localized compiler errors in place of naming conventions or late verifier/attach
failures. Section~\ref{sec:eval} measures whether those diagnostics trigger,
and Section~\ref{sec:limits} explains the limits of that evidence.

\section{Design and Implementation}
\label{sec:impl}

The compiler has three design goals. First, it must detect cross-boundary
errors at compile time, before the verifier or runtime sees them. Second, it
must preserve the existing Linux BPF toolchain so that type information,
portability, and verifier diagnostics remain available. Third, it must generate
idiomatic C that developers can inspect and debug.

The type system draws on established ideas: typestate tracks object state in
types~\cite{typestate}, and linear types enforce single-use resource
protocols~\cite{lineartypes,vault,sessiontypes}. \tool{} applies a lightweight
form of these ideas, distinguishing producer and consumer handles and
annotating execution domains, but does not attempt full substructural typing.

To meet these goals, the compiler (written in OCaml with dune) parses
\tool{} source, resolves imports and includes, builds symbol tables, runs
multi-program analysis and type checking, performs safety analysis, builds an
IR, and emits C. The compiler comprises nine modules covering maps, codegen,
safety, and struct\_ops. For an input \texttt{foo.ks},
the generated project contains \texttt{foo.ebpf.c}, \texttt{foo.c}, a
\texttt{Makefile}, and, when kfuncs are present, \texttt{foo.mod.c}. The
Makefile invokes bpftool to dump \texttt{vmlinux.h}, clang to produce a BPF
object, bpftool to generate a skeleton, and gcc to link the userspace loader
against libbpf (together with libelf and zlib).

Each typing rule of Section~\ref{sec:model} maps to a concrete compiler pass.
The compiler attaches a domain to each function as a scope refined by the
entry attribute, and the IR records each function's program type so that code
generation can dispatch the same source function to the eBPF or userspace
backend. Typed maps
lower to a single descriptor that records key and value types once, and both
backends read that descriptor to emit the eBPF \code{SEC(".maps")} definition
and the userspace accessor (Listing~\ref{lst:map}).
Lifecycle handles are enforced in the front end: only \code{load} yields a
\code{ProgramHandle}, and \code{attach} and \code{detach} require one, so a
misordered call fails type checking before code generation. The handle then
lowers to a file descriptor via libbpf APIs.

\begin{lstlisting}[language=C,caption={One typed map declaration lowers to both
a kernel \texttt{SEC(".maps")} definition and a userspace accessor from a single
descriptor.},label={lst:map}]
// BPFScript source
var counts : hash<u32, u64>(1024)

// generated eBPF C
struct {
  __uint(type, BPF_MAP_TYPE_HASH);
  __uint(max_entries, 1024);
  __type(key, __u32);
  __type(value, __u64);
} counts SEC(".maps");

// generated userspace C: same key/value types
__u32 key = ...; __u64 val;
bpf_map_lookup_elem(counts_fd, &key, &val);
\end{lstlisting}

The compiler does not replace the verifier, clang, bpftool, or libbpf, so
existing diagnostics remain available and any compatibility failure stays
visible. For interfaces that move faster than the DSL can stabilize,
\code{include} and \code{extern} declarations provide a fallback to raw header
declarations.

\section{Evaluation}
\label{sec:eval}

The evaluation answers three questions: \emph{(Q1)} does each typed
cross-boundary invariant trigger a compile-time diagnostic? \emph{(Q2)} does
one source generate a complete, buildable libbpf project, and does unifying it
reduce maintenance surface (footprint, change amplification, cross-boundary
synchronization)? \emph{(Q3)} does generated code load, attach, and run on a
real kernel? All experiments run on Linux \KSKernelVersion{} with clang 18,
bpftool 7.7, and libbpf 1.3.0. We report SLOC counts as nonblank, noncomment
lines, excluding generated headers.

\subsection{Q1: Compile-Time Diagnostic Coverage}

The compiler test suite contains \KSTotalUnitSuites{} suites and
\KSTotalUnitTests{} tests with no failures, covering all major subsystems from
lexer to code generation.

The central Q1 result is a suite of \KSStaticTotal{} targeted programs that exercises the diagnostics of
Section~\ref{sec:model} directly: one positive control plus
\KSStaticExpectedFailures{} expected rejections covering lifecycle misuses,
program-signature violations, map type errors, domain crossings, and safety
violations. Each case carries an expected diagnostic substring, so incidental
rejections do not count as passing. The suite shows that the invariants are implemented and stable.

We test whether \tool{} detects mistakes \emph{earlier} than the stock
toolchain. We inject \KSOrganicTotal{} realistic mistakes into a working XDP
map-counter, once in \tool{} and once in matched hand-written C/eBPF, and
record the earliest rejection stage along a shared ladder
(compile $<$ build $<$ verifier $<$ attach $<$ runtime $<$ undetected).
Table~\ref{tab:organic} reports the outcome. \tool{} rejects all
\KSOrganicTotal{} at compile time. The decisive case is the cross-boundary one:
an \texttt{@xdp} entry point handed a \texttt{*\_\_sk\_buff} context instead of
\texttt{*xdp\_md}. \tool{} rejects it from the program-signature rule of
Section~\ref{sec:model}. In C the program compiles, and because its body never
dereferences the context the verifier has no invalid access to flag, so the
mistake survives to a silently wrong load. The matched bodies deliberately touch
only a map, not a context field. A body that did read a context field could
trip the verifier on an out-of-range access, though still not as the
context-type confusion it is.

\begin{table}[H]
\centering
\caption{Earliest detection stage per injected mistake. \tool{} is strictly
earlier only on the cross-boundary context-type mistake; the rest are ties
clang already catches.}
\label{tab:organic}
\footnotesize
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.24\columnwidth}>{\raggedright\arraybackslash}p{0.24\columnwidth}>{\centering\arraybackslash}p{0.14\columnwidth}>{\centering\arraybackslash}p{0.18\columnwidth}@{}}
\toprule
Injected mistake & Coupling & BS & C/libbpf \\
\midrule
Wrong context type & signature/domain & \KSOrganicWrongContextKS{} & \KSOrganicWrongContextC{} \\
Undeclared map & representation & \KSOrganicMapUndeclaredKS{} & \KSOrganicMapUndeclaredC{} \\
String into \texttt{u64} value & representation & \KSOrganicMapValueTypeKS{} & \KSOrganicMapValueTypeC{} \\
Oversized stack buffer & safety & \KSOrganicStackOverflowKS{} & \KSOrganicStackOverflowC{} \\
\bottomrule
\end{tabular}
\end{table}

The
other \KSOrganicTie{} mistakes tie because clang already catches them locally:
an undeclared map, a string stored into a \texttt{u64} value, and an oversized
stack buffer. The result is therefore \KSOrganicKSEarlier{} earlier and
\KSOrganicTie{} tied: typing helps precisely on the cross-boundary relation that
local C checking cannot see. Section~\ref{sec:limits} explains the limits of
this small, author-injected set.

\FloatBarrier
\subsection{Q2: Project Generation}

We test whether \tool{} generates complete projects by compiling
\KSTotalExamples{} example programs. Of these, \KSKSCompileOK{} compile
successfully and \KSMakeOK{} produce working libbpf projects. The single
compile-time rejection is \texttt{safety\_demo.ks}, where safety analysis
detects 608 bytes of stack use (above the 512-byte eBPF limit) and halts
before codegen, catching a verifier-relevant problem early. The two examples
that compile but do not build are both struct\_ops programs whose
bpftool-generated skeletons reference a libbpf field absent from the host
headers (Section~\ref{sec:eval:q3} isolates this as toolchain version skew).
For examples that build successfully, the compiler emits a median of
\KSMedianGeneratedSloc{} lines from \KSMedianKSSloc{} source lines. Across
\KSSourceFootprintWorkloads{} workloads with hand-written C/libbpf baselines,
\tool{} sources total \KSSourceFootprintUniqueKSSloc{} lines versus
\KSSourceFootprintUniqueCSloc{}. Table~\ref{tab:features} shows the feature
coverage.

\begin{table}[H]
\centering
\caption{Example marker coverage. A = attempted, BS = \tool{} compiler success,
B = generated project build success.}
\label{tab:features}
\begin{tabular}{lrrr@{\hspace{0.8em}}lrrr}
\toprule
Feature & A & BS & B & Feature & A & BS & B \\
\midrule
XDP & \KSFeatXDPAttempt{} & \KSFeatXDPKS{} & \KSFeatXDPBuild{} &
TC & \KSFeatTCAttempt{} & \KSFeatTCKS{} & \KSFeatTCBuild{} \\
Probe & \KSFeatProbeAttempt{} & \KSFeatProbeKS{} & \KSFeatProbeBuild{} &
Tracept. & \KSFeatTracepointAttempt{} & \KSFeatTracepointKS{} & \KSFeatTracepointBuild{} \\
Perf & \KSFeatPerfAttempt{} & \KSFeatPerfKS{} & \KSFeatPerfBuild{} &
Maps & \KSFeatMapsAttempt{} & \KSFeatMapsKS{} & \KSFeatMapsBuild{} \\
Ringbuf & \KSFeatRingbufAttempt{} & \KSFeatRingbufKS{} & \KSFeatRingbufBuild{} &
Dynptr & \KSFeatDynptrAttempt{} & \KSFeatDynptrKS{} & \KSFeatDynptrBuild{} \\
kfunc & \KSFeatKfuncAttempt{} & \KSFeatKfuncKS{} & \KSFeatKfuncBuild{} &
Str.\_ops & \KSFeatStructOpsAttempt{} & \KSFeatStructOpsKS{} & \KSFeatStructOpsBuild{} \\
Tail & \KSFeatTailCallAttempt{} & \KSFeatTailCallKS{} & \KSFeatTailCallBuild{} &
User & \KSFeatUserspaceAttempt{} & \KSFeatUserspaceKS{} & \KSFeatUserspaceBuild{} \\
\bottomrule
\end{tabular}
\end{table}

We hand-ported \KSExternalPortWorkloads{} \texttt{xdp-tutorial} XDP programs
to \tool{}. The ports total
\KSExternalPortKSSloc{} lines while the original C/eBPF bodies total
\KSExternalPortCSloc{} lines, showing that \tool{} handles external code at
comparable line counts. A source-only scan of \KSExternalCorpusRepos{} public
eBPF repositories confirms that the program types and map patterns we generate
(XDP, TC, kprobe, ring buffers, struct\_ops) appear together in real projects,
supporting ecological validity.

Source-footprint totals show how much code a finished workload occupies, not
how many places a small requirement change touches. We measure this coupling
by constructing \KSChangeAmpCases{} matched micro-edits and comparing the diff
size of one \tool{} source against a hand-written C/libbpf split. The edits are the ones
the typed unified source is meant to centralize: changing a map from
\code{array} to \code{percpu_array}, changing a program from \code{@xdp} to
\code{@tc}, moving a \code{perf_event} attach from standalone to grouped,
extending a shared ringbuf event schema, and adding ringbuf reporting plus
userspace handling.
Table~\ref{tab:change-amp} reports modified file count, edit-site count,
changed LOC, and whether the author must manually keep kernel code,
userspace code, or skeleton/section conventions in sync.

{\setlength{\intextsep}{4pt}
\begin{table}[H]
\centering
\caption{Change amplification for \KSChangeAmpCases{} matched micro-edits. ``Sync'' reports
which manually synchronized surfaces change: kernel (K), userspace (U), or
skeleton/section conventions (S).}
\label{tab:change-amp}
\footnotesize
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{0.95}
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.31\columnwidth}ccccc@{}}
\toprule
Edit & Impl. & Files & Sites & LOC & Sync \\
\midrule
\shortstack[l]{Map \\ \code{array}$\rightarrow$\code{percpu_array}} & BS & \KSChangeAmpMapTypeFilesKS{} & \KSChangeAmpMapTypeSitesKS{} & \KSChangeAmpMapTypeLocKS{} & \KSChangeAmpMapTypeSyncKS{} \\
 & C & \KSChangeAmpMapTypeFilesC{} & \KSChangeAmpMapTypeSitesC{} & \KSChangeAmpMapTypeLocC{} & \KSChangeAmpMapTypeSyncC{} \\
\addlinespace[2pt]
\shortstack[l]{Program \\ \code{@xdp}$\rightarrow$\code{@tc}} & BS & \KSChangeAmpProgTypeFilesKS{} & \KSChangeAmpProgTypeSitesKS{} & \KSChangeAmpProgTypeLocKS{} & \KSChangeAmpProgTypeSyncKS{} \\
 & C & \KSChangeAmpProgTypeFilesC{} & \KSChangeAmpProgTypeSitesC{} & \KSChangeAmpProgTypeLocC{} & \KSChangeAmpProgTypeSyncC{} \\
\addlinespace[2pt]
\shortstack[l]{Perf event \\ standalone$\rightarrow$grouped} & BS & \KSChangeAmpPerfGroupFilesKS{} & \KSChangeAmpPerfGroupSitesKS{} & \KSChangeAmpPerfGroupLocKS{} & \KSChangeAmpPerfGroupSyncKS{} \\
 & C & \KSChangeAmpPerfGroupFilesC{} & \KSChangeAmpPerfGroupSitesC{} & \KSChangeAmpPerfGroupLocC{} & \KSChangeAmpPerfGroupSyncC{} \\
\addlinespace[2pt]
\shortstack[l]{Shared event \\ schema} & BS & \KSChangeAmpEventSchemaFilesKS{} & \KSChangeAmpEventSchemaSitesKS{} & \KSChangeAmpEventSchemaLocKS{} & \KSChangeAmpEventSchemaSyncKS{} \\
 & C & \KSChangeAmpEventSchemaFilesC{} & \KSChangeAmpEventSchemaSitesC{} & \KSChangeAmpEventSchemaLocC{} & \KSChangeAmpEventSchemaSyncC{} \\
\addlinespace[2pt]
\shortstack[l]{Add ringbuf \\ reporting/handling} & BS & \KSChangeAmpUserspaceFilesKS{} & \KSChangeAmpUserspaceSitesKS{} & \KSChangeAmpUserspaceLocKS{} & \KSChangeAmpUserspaceSyncKS{} \\
 & C & \KSChangeAmpUserspaceFilesC{} & \KSChangeAmpUserspaceSitesC{} & \KSChangeAmpUserspaceLocC{} & \KSChangeAmpUserspaceSyncC{} \\
\bottomrule
\end{tabular}
\end{table}
}

The structural result is decisive. None of the \tool{} edits require manual
cross-boundary synchronization, whereas hand-written C/libbpf requires
userspace synchronization in \KSChangeAmpUserspaceSyncCasesC{}/\KSChangeAmpCases{}
cases, kernel-side synchronization in
\KSChangeAmpKernelSyncCasesC{}/\KSChangeAmpCases{}, and skeleton or
section-convention synchronization in
\KSChangeAmpSkeletonSyncCasesC{}/\KSChangeAmpCases{}. This directly shows that
a unified typed source reduces change amplification, not only total lines. The
diff-size counts point the same way. Across the \KSChangeAmpCases{} edits,
\tool{} has medians of \KSChangeAmpMedianFilesKS{} modified file,
\KSChangeAmpMedianSitesKS{} edit sites, and \KSChangeAmpMedianLocKS{} changed
lines, against \KSChangeAmpMedianFilesC{}, \KSChangeAmpMedianSitesC{}, and
\KSChangeAmpMedianLocC{} for the matched C/libbpf versions. With only
\KSChangeAmpCases{} edits we treat these as illustrative, not as a statistical
estimate.

Table~\ref{tab:evolution} compares four stages that add progressively richer
cross-boundary structure: pass-only XDP, then a counter map, then ring-buffer
reporting, and finally a separate struct\_ops callback workload. The
struct\_ops stage serves as a higher-complexity reference point, not the next
step of the same program.

\begin{table}[H]
\centering
\caption{Staged cross-boundary growth: maintained source footprint for a local
matched XDP progression plus a separate struct\_ops reference point. C totals
include hand-written userspace when the feature requires it.}
\label{tab:evolution}
\footnotesize
\begin{tabular}{@{}lrrrr@{}}
\toprule
Stage & BS & C eBPF & C user & C total \\
\midrule
Pass-only XDP & \KSEvolutionPassKSSloc{} & \KSEvolutionPassCEbpfSloc{} & \KSEvolutionPassCUserSloc{} & \KSEvolutionPassCTotalSloc{} \\
Add counter map & \KSEvolutionCountKSSloc{} & \KSEvolutionCountCEbpfSloc{} & \KSEvolutionCountCUserSloc{} & \KSEvolutionCountCTotalSloc{} \\
Add ringbuf reporting & \KSEvolutionRingbufKSSloc{} & \KSEvolutionRingbufCEbpfSloc{} & \KSEvolutionRingbufCUserSloc{} & \KSEvolutionRingbufCTotalSloc{} \\
Shift to struct\_ops callbacks & \KSEvolutionStructOpsKSSloc{} & \KSEvolutionStructOpsCEbpfSloc{} & \KSEvolutionStructOpsCUserSloc{} & \KSEvolutionStructOpsCTotalSloc{} \\
\bottomrule
\end{tabular}
\end{table}

The external \texttt{xdp-tutorial} ports provide supporting evidence for the
early XDP stages (the \KSExternalPortWorkloads{} pinned programs span
\texttt{basic01-xdp-pass} through \texttt{basic03-map-counter}).
Table~\ref{tab:evolution} itself reports a local matched
comparison. At each XDP stage, \tool{} grows modestly
(\KSEvolutionPassToCountKSDelta{} lines for the counter map,
\KSEvolutionCountToRingbufKSDelta{} for ring-buffer reporting), while the
matched C/libbpf baseline grows faster (\KSEvolutionPassToCountCDelta{} and
\KSEvolutionCountToRingbufCDelta{} respectively). The separate struct\_ops
reference point adds \KSEvolutionRingbufToStructOpsKSDelta{} lines on the
\tool{} side versus \KSEvolutionRingbufToStructOpsCDelta{} in C/libbpf. By
the reporting stage the maintained baseline is \KSEvolutionRingbufRatio{} the
\tool{} source size, and the struct\_ops reference point reaches
\KSEvolutionStructOpsRatio{}. The abstraction-fit result is therefore limited
but direct: within one XDP family and again at a more complex cross-domain
reference point, the DSL mostly extends one file, while C/libbpf accumulates
more coordination code and userspace surface.

\subsection{Q3: Runtime Validation}
\label{sec:eval:q3}

We test a progression of increasingly stringent checks: build success, kernel
acceptance, attachability, and runtime behavior. Table~\ref{tab:real}
summarizes the results on a real kernel.

\begin{table}[!ht]
\centering
\caption{Real-kernel checks: compile, build, verifier-load, and attach.}
\label{tab:real}
\begin{tabular}{lrr}
\toprule
Stage & Population & Pass \\
\midrule
\tool{} compile & \KSTotalExamples{} & \KSKSCompileOK{} \\
Generated project build & \KSTotalExamples{} & \KSMakeOK{} \\
Verifier-load (strict) & \KSVerifierBuildObjects{} & \KSVerifierBuildLoadOK{} \\
Isolated XDP attach/detach & \KSAttachEligible{} & \KSAttachOK{} \\
\bottomrule
\end{tabular}
\end{table}

Most generated objects pass the verifier: \KSVerifierBuildLoadOK{} of
\KSVerifierBuildObjects{} from fully built projects load successfully. The
\KSVerifierBuildLoadFailed{} failures are a reference-ownership leak, a
map-creation failure, a missing kfunc symbol, and an object with no program
section. For XDP, \KSAttachOK{} of \KSAttachEligible{} objects attach and
detach successfully. The workloads below exercise TC and struct\_ops.

Beyond load and attach, matched workloads confirm the generated objects
\emph{run}. Under \code{BPF_PROG_TEST_RUN}, a minimal
generated XDP pass program matches its hand-written baseline in instruction
count and latency. Over \KSTrafficTrials{} veth/iperf3 trials, generated and
hand-written XDP pass programs deliver comparable throughput, and matched TC
programs pass traffic as well. These numbers are sanity checks confirming
functional equivalence, not performance claims.

Preserving the kernel toolchain means \tool{} also inherits its compatibility
coupling. The two struct\_ops
build failures are not parser or type errors: both compile to valid eBPF
objects but fail because the bpftool-generated skeleton references a libbpf
field absent from the installed headers, reflecting mismatched toolchain
versions rather than an inherent limitation. A direct libbpf runner (bypassing skeletons) loads
both the generated and a matched hand-written tcp-congestion struct\_ops object
successfully. The failure is a version-skew problem in the generated
\emph{skeleton}, exactly the compatibility coupling of Section~\ref{sec:bg}
that source unification does not eliminate.

\section{Limitations}
\label{sec:limits}

The differential probe injects only \KSOrganicTotal{} hand-chosen mistakes. A
stronger study would mine real eBPF bugs from revision histories. Lifecycle
typing enforces a producer/consumer handle distinction, not full typestate, so
use-after-detach and leaked handles are not statically ruled out. The corpus
is author-written, and the external port shows that line-count savings vary
when porting existing code rather than writing from scratch. The change-amplification
study uses small checked-in micro-edits rather than real revision histories,
so it measures maintenance-surface coupling rather than true developer time or
debugging effort. Traffic numbers are local
sanity checks, not performance claims. Finally, the struct\_ops build
failures reflect bpftool/libbpf version skew rather than a deep limitation,
but they show that generated builds depend on toolchain alignment.

\section{Related Work}

Prior work on eBPF development falls into three categories. Domain-specific
DSLs target particular use cases: bpftrace for tracing~\cite{bpftrace}, BPFBox
and ActPlane for security policies~\cite{bpfbox,actplane}, BMC for
caching~\cite{bmc}, P4c-eBPF for networking~\cite{p4c-ebpf}, and Yaksha-Prashna
for bytecode analysis~\cite{yaksha}. These DSLs generate only kernel code and
do not address the cross-boundary structure that \tool{} targets. General eBPF
development tools include the standard eBPF development workflow~\cite{libbpf}, Aya
and Rex for Rust~\cite{aya,rex}, Wasm-bpf for WebAssembly
deployment~\cite{wasmbpf}, and eunomia-bpf for dynamic
loading~\cite{eunomia-bpf}. These reduce boilerplate but leave lifecycle and
representation relationships implicit. Verified approaches take a different
direction: BeePL generates formally verified bytecode~\cite{beepl}, and Jitk
and Serval verify JIT correctness~\cite{jitk,serval}. \tool{} occupies a
middle ground: it generates both kernel and userspace code from one source and
types cross-boundary relationships, but targets practical integration with the
existing toolchain rather than formal verification.

\section{Conclusion}

\tool{} treats the cross-boundary structure of an eBPF application (program
lifecycle, shared maps, and execution domains) as something a compiler can type
rather than something developers hold together by convention and discover at
verifier-load or attach time. The evaluation confirms that each typed invariant
triggers a compile-time diagnostic, that one source generates buildable libbpf
projects, and that generated objects load, attach, and pass traffic on a real
kernel. The result is a concrete design point: typing eBPF's cross-boundary
structure is feasible, and because the compiler lowers through the stock
toolchain, both what the type system provides and the version-skew limits that
source unification cannot absorb stay visible and measurable.

\newpage
\bibliographystyle{ACM-Reference-Format}
\bibliography{references}

\end{document}
