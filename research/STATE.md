Current stage: paper-polish/review
Last updated: 2026-06-13
Active claim(s): C1, C2, C3, C4
Latest artifact: results/external_port_summary.json
Blocking gate: Whole-paper logic review must-fix findings have been patched in paper/kernelscript-paper.tex. Generated-loader evidence now includes a 20-trial perf_event lifecycle latency check, version-aware skeleton handling has local map-link repair evidence, tcp-congestion struct_ops has a loopback TCP workload check, a callback-flag workload shows the clean loopback transfer reaches cong_avoid and cwnd_event in generated and C/eBPF objects, a 5% loss-injected callback profile reaches ssthresh, cong_avoid, set_state, and cwnd_event in generated and C/eBPF objects, and the targeted static corpus now covers 28 cases including helper-scope, kernel-context allocation, and perf-event group-bound violations. The scheduler-extension lowering has been repaired for verifier-load evidence, and a separate opt-in harness now registers generated and C/eBPF toy FIFO schedulers, runs five bounded CPU progress trials while sched_ext remains enabled, records per-worker iteration counts, unregisters both, and returns sched_ext to disabled with zero rejected tasks. The source-footprint proxy now covers 11 matched local workload rows: unique KernelScript application sources total 203 SLOC, matching C/eBPF object sources total 254 SLOC, and C/libbpf baseline sources total 1105 SLOC with runner or loader files included. The external source-corpus scan covers 3 pinned public eBPF repositories, 166 selected C/header files, 34843 SLOC, and 14 tracked feature families; its 7-file manual classifier spot-check has zero false-positive or false-negative feature labels, but it remains source-only context. The external port check manually ports pinned `xdp-tutorial/basic01-xdp-pass`, `basic02-prog-by-name`, and `basic03-map-counter`: the KernelScript ports build through generated Makefiles, the original C/eBPF sources compile directly with clang, all 6 XDP objects attach, 5 one-second traffic trials pass per variant, and the map-counter pair increments the XDP_PASS map key. The top-systems writing and experiment checklist recorded in research/PAPER_LOGIC_REVIEW.md has no current known must-fix claim/evidence contradiction after the latest scope-wording, classifier-audit, baseline-provenance, and claim-first evaluation edits. The remaining gates are still evidence limits rather than known claim/evidence contradictions: broader external application translation/build/runtime or developer-time evidence beyond the small hand-ported XDP tutorial portfolio, scheduler-extension policy/performance evidence beyond one toy progress/fairness proxy, every tcp-congestion callback path beyond the two tested profiles, broader generated-dispatch-loop throughput beyond one perf_event lifecycle loader workload, upstream-integrated and cross-version skeleton generation, and non-local or longer-duration deployment evidence.
Next action: Expand external application translation/build/runtime evidence beyond the small manual XDP tutorial portfolio or add a developer-effort study for C1, add scheduler-extension policy/performance evidence beyond the toy progress/fairness proxy, expand tcp-congestion callback-path coverage, broaden generated-dispatch-loop throughput, integrate cross-version skeleton handling, or add non-local/longer-duration deployment evidence before strengthening runtime or portability claims.
